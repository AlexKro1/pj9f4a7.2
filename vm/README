# ASIX2 PJ9 2024-2025 FJE CLOT

## Requisits

Requereix tenir instal·lat previament:
1. Git https://git-scm.com/
2 Virtualbox https://www.virtualbox.org/
3. Vagrant https://www.vagrantup.com/

## Pasos a seguir

1. Inicia la màquina virtual amb les ordres de `Vagrant`:

	vagrant up

1.5. Si volem veure l'estat de la màquina virtual (des de la física):
	
	vagrant status


2. Accés a la màquina virtual:

	vagrant ssh

3. Si volem sortir de la màquina (des de dins de la màquina):
	
	exit

4. Si volem aturar la màquina:

	vagrant halt

5. Si volem esborrar la màquina virtual (des de la màquina física):

	vagrant destroy
